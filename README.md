# Домашнее задание по Java

## Цель:
Практика с языком программирования Java.

---

## Описание / Инструкция по выполнению задания:

### Родительский класс `Animal`
Свойства класса:
- **name** (`String`)
- **age** (`int`)
- **weight** (`double`)
- **color** (`String`)

Методы класса:
- **Getters & Setters**
- **`say()`**: выводит сообщение `"Я говорю"`
- **`go()`**: выводит сообщение `"Я иду"`
- **`drink()`**: выводит сообщение `"Я пью"`
- **`eat()`**: выводит сообщение `"Я ем"`

#### Метод `toString()`
Переопределенный метод возвращает строку вида:
Привет! Меня зовут {name}, мне {age} лет(/год/года), я вешу - {weight} кг, мой цвет - {color}

Количество лет определяется следующим образом:
- 1 → *«год»*
- 2, 3, 4 → *«года»*
- Остальные случаи → *«лет»*

Пример вывода:
Привет! Меня зовут Бобик, мне 5 лет, я вешу - 15 кг, мой цвет - черный.

---

### Интерфейс `Flying`
Метод интерфейса:
- **`fly()`**: выводит сообщение `"Я лечу"`

---

### Класс `Cat`, наследуемый от `Animal`
- Переопределение метода **`say()`**: выводит сообщение `"Мяу"`.

---

### Класс `Dog`, наследуемый от `Animal`
- Переопределение метода **`say()`**: выводит сообщение `"Гав"`.

---

### Класс `Duck`, наследуемый от `Animal`, реализует интерфейс `Flying`
- Переопределение метода **`say()`**: выводит сообщение `"Кря"`.
- Реализация метода **`fly()`**: выводит сообщение `"Я лечу"`.

---

### Основная программа
Используется коллекция **ArrayList\<Animal\>**. Меню создается через консоль с командами: **add**, **list**, **exit**.

- Команда **`add`**: запрашивает название животного (**cat**, **dog**, **duck**) и просит ввести имя, возраст, вес и цвет. После инициализации экземпляра вызывает метод **`say()`**.
- Команда **`list`**: выводит на экран результат метода **`toString()`** для каждого элемента коллекции.
- Команда **`exit`**: выход из приложения.

Команды оформлены в виде **enum** и нечувствительны к регистру, лишняя пунктуация удаляется.

---

## Компетенции:
- Основы синтаксиса Java
- Использование абстрактных классов (рекомендована практика)

---

Рекомендуемый срок сдачи: **до 16 декабря 2025 г.**

---

## Правки:

| Дата           | Изменения                                                                                                                                                                                                                                |
|----------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **06.12.2025** | Из `.gitignore`: исключены правила для игнорирования скомпилированных файлов внутри директорий `/src`.                                                                                                                                   |
| **07.12.2025** | Переименование константы `NAMES` в `COMMAND`.<br>- Добавлен цикл проверки имени животного на пустоту.<br>- Обработка клички животного с заглавной буквы перед добавлением в коллекцию.<br>- Проверка правильности ввода возраста и веса. |
| **08.12.2025** | Исключены правила `.gitignore` для IDE (VSCode, Netbeans, Eclipse). Включена директория .idea/.                                                                                                                                         |
| **09.12.2025** | Отформатирован файл README.                                                                                                                                       

---